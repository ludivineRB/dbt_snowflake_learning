# 06 - Gestion Multi-Provider (Aliases)

## Objectif
Apprendre à gérer des ressources dans différentes régions ou projets au sein d'un même code Terraform.

## Concepts
- L'argument `alias` dans le bloc `provider`.
- L'argument `provider` dans la ressource pour spécifier quel alias utiliser.
